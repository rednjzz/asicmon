class Miner  {
    constructor(name, port, host){
        this.name       = name;
        this.host       = host;
        this.type       = type;
    }
    command(cmd) {
        var
    }

}